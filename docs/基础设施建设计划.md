# 前端基础设施建设计划

## 一、建设目标

### 核心价值
- **提升开发效率**：减少重复劳动，快速开发新功能
- **保证代码质量**：统一规范，降低Bug率
- **降低维护成本**：代码可读性强，易于维护
- **支撑业务扩展**：架构灵活，可快速响应需求变化

### 建设原则
- **够用即可**：不过度设计，优先解决实际问题
- **渐进增强**：分阶段建设，快速迭代
- **文档先行**：配套文档，降低学习成本
- **团队共建**：鼓励团队贡献，持续优化

---

## 二、基础设施全景图

```
基础设施
├── 项目架构
│   ├── 脚手架与目录结构
│   ├── 技术栈配置
│   └── 环境管理
├── 工程化体系
│   ├── 代码规范（ESLint/Prettier）
│   ├── Git 规范（Husky/Commitlint）
│   ├── CI/CD 流程
│   └── 测试体系
├── 组件库与设计系统
│   ├── 基础 UI 组件
│   ├── 业务组件
│   ├── 设计规范
│   └── 组件文档（Storybook）
├── 开发工具链
│   ├── CLI 脚手架
│   ├── 代码生成器
│   ├── VS Code 配置
│   └── 调试工具
├── 公共能力
│   ├── API 请求封装
│   ├── 状态管理
│   ├── 路由管理
│   ├── 权限控制
│   └── 工具函数库
└── 文档与培训
    ├── 开发指南
    ├── 最佳实践
    ├── 技术分享
    └── FAQ
```

---

## 三、详细建设内容

### 3.1 项目架构

#### 脚手架搭建
```bash
# 技术栈
- Next.js 16 (App Router)
- React 19
- TypeScript 5
- Tailwind CSS 4
```

#### 目录结构
```
project-root/
├── app/                  # Next.js App Router
│   ├── (auth)/           # 路由分组
│   │   ├── login/
│   │   └── register/
│   ├── (dashboard)/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── api/              # API Routes
│   ├── layout.tsx        # 根布局
│   ├── page.tsx          # 首页
│   └── globals.css       # 全局样式
├── components/           # 组件库
│   ├── ui/               # 基础 UI 组件
│   │   ├── button.tsx
│   │   ├── input.tsx
│   │   └── modal.tsx
│   ├── business/         # 业务组件
│   │   ├── user-card.tsx
│   │   └── data-table.tsx
│   └── layouts/          # 布局组件
│       ├── header.tsx
│       └── sidebar.tsx
├── hooks/                # 自定义 Hooks
│   ├── use-auth.ts
│   ├── use-fetch.ts
│   └── use-pagination.ts
├── services/             # API 调用
│   ├── api.ts            # 统一请求封装
│   ├── auth.service.ts
│   └── user.service.ts
├── stores/               # 状态管理
│   ├── auth.store.ts
│   └── user.store.ts
├── types/                # 类型定义
│   ├── api.types.ts
│   ├── user.types.ts
│   └── common.types.ts
├── utils/                # 工具函数
│   ├── format.ts
│   ├── validation.ts
│   └── constants.ts
├── lib/                  # 第三方库配置
│   ├── sentry.ts
│   └── tracker.ts
├── public/               # 静态资源
├── styles/               # 样式文件
├── tests/                # 测试文件
│   ├── unit/
│   └── e2e/
├── docs/                 # 文档
├── .env.local            # 环境变量
├── .env.production
├── next.config.ts        # Next.js 配置
├── tsconfig.json         # TypeScript 配置
├── tailwind.config.ts    # Tailwind 配置
├── eslint.config.mjs     # ESLint 配置
└── package.json
```

#### 环境管理
```bash
# 环境变量文件
.env.local              # 本地开发
.env.development        # 开发环境
.env.test               # 测试环境
.env.staging            # 预发布环境
.env.production         # 生产环境

# 环境变量示例
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_APP_NAME=My App
NEXT_PUBLIC_ENV=development
DATABASE_URL=postgresql://...
SENTRY_DSN=https://...
```

---

### 3.2 工程化体系

#### 代码规范
```json
// .eslintrc.json
{
  "extends": [
    "next/core-web-vitals",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "no-console": ["warn", { "allow": ["warn", "error"] }],
    "@typescript-eslint/no-unused-vars": "error",
    "@typescript-eslint/no-explicit-any": "warn"
  }
}
```

```json
// .prettierrc
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

#### Git 规范
```javascript
// .husky/pre-commit
npm run lint
npm run type-check

// .husky/commit-msg
npx --no -- commitlint --edit $1
```

```javascript
// commitlint.config.js
module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    'type-enum': [
      2,
      'always',
      [
        'feat',     // 新功能
        'fix',      // Bug 修复
        'docs',     // 文档更新
        'style',    // 代码格式（不影响代码运行）
        'refactor', // 重构
        'perf',     // 性能优化
        'test',     // 测试
        'chore',    // 构建过程或辅助工具的变动
      ],
    ],
  },
};

// 提交示例
// feat: 添加用户登录功能
// fix: 修复登录按钮点击无响应的问题
// docs: 更新 API 文档
```

#### CI/CD 流程
```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint
        run: npm run lint
      
      - name: Type check
        run: npm run type-check
      
      - name: Unit tests
        run: npm run test
      
      - name: Build
        run: npm run build
      
      - name: E2E tests
        run: npm run test:e2e
```

#### 测试体系
```typescript
// 单元测试 - Jest + Testing Library
// __tests__/components/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { Button } from '@/components/ui/button';

describe('Button', () => {
  it('renders correctly', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = jest.fn();
    render(<Button onClick={handleClick}>Click me</Button>);
    fireEvent.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
});
```

```typescript
// E2E 测试 - Playwright
// tests/e2e/login.spec.ts
import { test, expect } from '@playwright/test';

test('user can login', async ({ page }) => {
  await page.goto('/login');
  
  await page.fill('[name="email"]', 'user@example.com');
  await page.fill('[name="password"]', 'password123');
  await page.click('[type="submit"]');
  
  await expect(page).toHaveURL('/dashboard');
  await expect(page.locator('text=Welcome')).toBeVisible();
});
```

---

### 3.3 组件库与设计系统

#### 基础 UI 组件
基于 shadcn/ui 定制：
- Button（按钮）
- Input（输入框）
- Select（选择器）
- Modal/Dialog（对话框）
- Toast（提示消息）
- Loading（加载状态）
- Tabs（标签页）
- Dropdown（下拉菜单）
- Tooltip（工具提示）
- Badge（徽章）
- Card（卡片）
- Table（表格）

#### 业务组件
根据业务场景沉淀：
- DataTable（数据表格 + 分页 + 筛选 + 排序）
- SearchForm（搜索表单）
- UserCard（用户卡片）
- ImageUploader（图片上传）
- RichTextEditor（富文本编辑器）
- Chart（图表封装）

#### Design Token
```typescript
// styles/tokens.ts
export const tokens = {
  colors: {
    primary: {
      50: '#f0f9ff',
      100: '#e0f2fe',
      // ...
      900: '#0c4a6e',
    },
    gray: { /* ... */ },
    success: { /* ... */ },
    error: { /* ... */ },
  },
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '16px',
    lg: '24px',
    xl: '32px',
  },
  typography: {
    fontSize: {
      xs: '12px',
      sm: '14px',
      base: '16px',
      lg: '18px',
      xl: '20px',
    },
    fontWeight: {
      normal: 400,
      medium: 500,
      semibold: 600,
      bold: 700,
    },
  },
  borderRadius: {
    none: '0',
    sm: '4px',
    md: '8px',
    lg: '12px',
    full: '9999px',
  },
};
```

#### Storybook 文档
```typescript
// Button.stories.tsx
import type { Meta, StoryObj } from '@storybook/react';
import { Button } from './button';

const meta: Meta<typeof Button> = {
  title: 'UI/Button',
  component: Button,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<typeof Button>;

export const Primary: Story = {
  args: {
    children: 'Button',
    variant: 'primary',
  },
};

export const Secondary: Story = {
  args: {
    children: 'Button',
    variant: 'secondary',
  },
};
```

---

### 3.4 开发工具链

#### CLI 脚手架
```bash
# 快速生成页面
npm run generate:page user-profile
# 生成：
# - app/user-profile/page.tsx
# - app/user-profile/loading.tsx
# - app/user-profile/error.tsx

# 快速生成组件
npm run generate:component UserCard
# 生成：
# - components/business/user-card.tsx
# - components/business/user-card.test.tsx
# - components/business/user-card.stories.tsx

# 快速生成 API Service
npm run generate:service user
# 生成：
# - services/user.service.ts
# - types/user.types.ts
```

#### 代码生成器
```typescript
// scripts/generate-component.ts
import fs from 'fs';
import path from 'path';

const componentTemplate = (name: string) => `
import React from 'react';

interface ${name}Props {
  // props 定义
}

export function ${name}({}: ${name}Props) {
  return (
    <div>
      {/* 组件内容 */}
    </div>
  );
}
`;

// 使用：node scripts/generate-component.ts UserCard
```

#### VS Code 配置
```json
// .vscode/settings.json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "files.associations": {
    "*.css": "tailwindcss"
  }
}
```

```json
// .vscode/extensions.json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "bradlc.vscode-tailwindcss",
    "ms-playwright.playwright"
  ]
}
```

---

### 3.5 公共能力

#### API 请求封装
```typescript
// services/api.ts
import axios from 'axios';

const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  timeout: 10000,
});

// 请求拦截器
api.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
api.interceptors.response.use(
  (response) => response.data,
  async (error) => {
    if (error.response?.status === 401) {
      // Token 过期，尝试刷新
      await refreshToken();
      return api.request(error.config);
    }
    return Promise.reject(error);
  }
);

export { api };
```

#### 状态管理
```typescript
// stores/auth.store.ts - 使用 Zustand
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  user: User | null;
  token: string | null;
  login: (user: User, token: string) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      token: null,
      login: (user, token) => set({ user, token }),
      logout: () => set({ user: null, token: null }),
    }),
    {
      name: 'auth-storage',
    }
  )
);
```

#### 路由管理
```typescript
// lib/routes.ts
export const routes = {
  home: '/',
  login: '/login',
  dashboard: '/dashboard',
  userProfile: (id: string) => `/users/${id}`,
  // ...
} as const;

// 使用
import Link from 'next/link';
import { routes } from '@/lib/routes';

<Link href={routes.userProfile('123')}>User Profile</Link>
```

#### 权限控制
```typescript
// components/auth/ProtectedRoute.tsx
'use client';

import { useAuthStore } from '@/stores/auth.store';
import { useRouter } from 'next/navigation';
import { useEffect } from 'react';

export function ProtectedRoute({ children, requiredRole }: Props) {
  const { user } = useAuthStore();
  const router = useRouter();

  useEffect(() => {
    if (!user) {
      router.push('/login');
    } else if (requiredRole && !user.roles.includes(requiredRole)) {
      router.push('/403');
    }
  }, [user, requiredRole, router]);

  if (!user) return null;
  return <>{children}</>;
}
```

#### 工具函数库
```typescript
// utils/format.ts
export const formatDate = (date: string | Date) => {
  // 日期格式化
};

export const formatCurrency = (amount: number) => {
  // 货币格式化
};

// utils/validation.ts
export const isEmail = (email: string) => {
  // 邮箱验证
};

export const isPhone = (phone: string) => {
  // 手机号验证
};

// utils/debounce.ts
export const debounce = <T extends (...args: any[]) => any>(
  func: T,
  wait: number
) => {
  // 防抖函数
};
```

---

## 四、建设排期

### Week 1: 项目初始化
- [x] Next.js 脚手架搭建
- [x] 目录结构规划
- [x] TypeScript 配置
- [x] Tailwind CSS 配置
- [x] 环境变量管理

### Week 1-2: 工程化配置
- [x] ESLint + Prettier 配置
- [x] Husky + Commitlint 配置
- [x] CI/CD 基础流程
- [x] 测试框架（Jest + Playwright）

### Week 2: 基础组件库
- [ ] 引入 shadcn/ui
- [ ] 定制主题配置
- [ ] 封装 10+ 基础组件
- [ ] Storybook 配置

### Week 2-3: 公共能力
- [ ] API 请求封装
- [ ] 状态管理方案落地
- [ ] 路由管理
- [ ] 权限控制
- [ ] 工具函数库

### Week 3: 开发工具
- [ ] CLI 脚手架开发
- [ ] 代码生成器
- [ ] VS Code 配置共享
- [ ] 开发文档编写

### 持续优化（贯穿始终）
- [ ] 业务组件沉淀
- [ ] 文档完善
- [ ] 最佳实践总结
- [ ] 团队培训

---

## 五、衡量标准

### 开发效率指标
- **新页面开发时间**：< 1天（使用脚手架）
- **组件复用率**：> 60%
- **代码生成覆盖率**：> 40%

### 代码质量指标
- **ESLint 错误**：0
- **TypeScript 类型覆盖**：100%
- **单元测试覆盖率**：> 60%
- **Code Review 通过率**：> 95%

### 性能指标
- **构建时间**：< 2分钟
- **热更新时间**：< 1秒
- **首次加载 JS**：< 200KB

---

## 六、文档与培训

### 必备文档
1. **快速开始指南**：新人 1 小时上手
2. **开发规范**：代码风格、命名规范、Git 规范
3. **组件库文档**：Storybook + 使用示例
4. **API 对接文档**：接口规范、调用示例
5. **常见问题 FAQ**：疑难问题解答

### 培训计划
- **新人培训**：项目架构、开发流程、工具使用
- **技术分享会**：双周一次，分享最佳实践
- **Code Review**：每周定期进行，作为培训机会

---

## 七、持续优化

### 反馈机制
- **开发者体验调研**：每月收集反馈
- **痛点收集**：建立问题反馈渠道
- **优化迭代**：根据反馈持续优化

### 技术债务管理
- **定期Review**：每季度评估技术债务
- **优先级排序**：区分紧急 vs 重要
- **专项时间**：预留 20% 时间还技术债

---

## 附录：工具清单

### 开发工具
- **IDE**: VS Code
- **包管理**: npm / pnpm
- **版本控制**: Git + GitHub/GitLab
- **API 测试**: Postman / Insomnia

### 质量保障
- **代码规范**: ESLint + Prettier
- **类型检查**: TypeScript
- **单元测试**: Jest + Testing Library
- **E2E 测试**: Playwright
- **代码审查**: GitHub PR Review

### 监控分析
- **错误监控**: Sentry
- **性能监控**: Lighthouse CI
- **埋点分析**: Google Analytics / 神策

### 协作工具
- **文档**: Notion / 语雀
- **设计稿**: Figma
- **项目管理**: Jira / Linear
- **沟通**: Slack / 飞书


